@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Device
<h2>
    </h2>
    <div class="d-flex justify-content-center">
    <form asp-action="Edit" class="w-50">

        <input asp-for="@Model.Id" type="hidden" />
           <div class="form-group">
            <label asp-for="@Html.">@Html.DisplayNameFor(name=>name.Name)</label>
            <input class="form-control" value="@Model.Name" asp-for="Name" />
            <span asp-validation-for="Name" class="text-danger"></span>

           </div>
        <div class="form-group">
            <label asp-for="@Html.">@Html.DisplayNameFor(name=>name.Icon)</label>
            <input class="form-control" value="@Model.Icon" asp-for="Icon" />
        </div>
        <input type="submit" value="Post" class=" btn btn-success mt-2" />

@*         <a type="submit" class="btn btn-success" asp-action="Index" asp-controller="Devices">Create</a>
 *@       </form>
    </div>